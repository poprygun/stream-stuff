spring:
  zipkin:
    base-url: http://localhost:9411/
    enabled: true
    sender:
      type: web

  sleuth:
    sampler:
      probability: 1.0

  application:
    name: http-listener
---
spring:
  profiles:
    active: k8s

  zipkin:
    base-url: http://zipkin:9411/

logging:
  level:
    org.springframework.web: debug