
spring:
  cloud:
    stream:
      bindings:
        soundbitsChannel:
          destination: soundbits

  zipkin:
    base-url: http://localhost:8080/
    enabled: true
    sender:
      type: web

  sleuth:
    sampler:
      probability: 1.0

  application:
    name: publisher
server:
  port: 0
